{
  "id": "T002",
  "kind": "implementation",
  "title": "Scaffold browser runtime and project verification",
  "status": "blocked",
  "priority": "urgent",
  "depends_on": ["T001"],
  "areas": ["web-runtime", "tooling"],
  "requirements": ["FR-013", "QR-001", "QR-005"],
  "requires": ["repo-write", "shell"],
  "prefers": ["browser-interaction"],
  "acceptance": [
    "a TypeScript + Vite browser application builds and opens with a Three.js scene using a WebGL2-compatible rendering baseline",
    "Rapier 3D/WASM is initialized behind a small physics boundary without coupling game logic directly to package bootstrap details",
    "action-based keyboard input exposes steer, throttle, brake/reverse, handbrake, and reset actions even if vehicle behavior is still placeholder",
    "package scripts provide deterministic typecheck/test/build checks and .agent/VERIFY.json is updated so quick verification includes product-aware checks",
    "third-party runtime dependencies are pinned and their licenses are recorded or mechanically discoverable for later attribution review"
  ],
  "verify": "npm run check",
  "verification": null,
  "claim": null,
  "handoff": "BLOCKED: Implementation is committed on roach/T002-chatgpt-nydrive-a830 and PR #1. Strict local TypeScript validation passes after fixing an app-root narrowing defect, but the declared npm run check cannot be executed honestly: this shell cannot resolve npm/GitHub network hosts, and repeated GitHub Actions jobs terminate with runner_id 0 and zero steps. Resume once an npm-capable runner/environment is available; run npm install --ignore-scripts && npm run check, visually/browser-smoke the Three.js/Rapier boot if possible, then integrate and finish T002.",
  "review_of": null,
  "correction_of": null,
  "integration": null,
  "evidence": null,
  "completed_at": null,
  "commit": null
}
